
{
  "name": "experience_assignments",
  "options": {
    "validator": {
      "$jsonSchema": {
        "bsonType": "object",
        "required": ["user_code", "experience_code", "assigned_by", "status", "created_at"],
        "properties": {
          "user_code": {
            "bsonType": "string",
            "description": "회원 코드"
          },
          "experience_code": {
            "bsonType": "string",
            "description": "체험단 코드"
          },
          "assigned_by": {
            "bsonType": "string",
            "description": "할당한 관리자"
          },
          "status": {
            "bsonType": "string",
            "enum": ["assigned", "joined", "completed", "expired"],
            "description": "할당 상태"
          },
          "deadline": {
            "bsonType": "string",
            "title": "datetime",
            "description": "참여 마감일"
          },
          "notes": {
            "bsonType": "string",
            "description": "할당 메모"
          },
          "created_at": {
            "bsonType": "string",
            "title": "datetime",
            "description": "할당일시"
          },
          "updated_at": {
            "bsonType": "string",
            "title": "datetime",
            "description": "상태 변경일시"
          }
        },
        "additionalProperties": true
      }
    },
    "validationLevel": "moderate",
    "validationAction": "warn"
  }
}
